#!/bin/bash
# {{ ansible_managed }}
# All manual changes will be reverted by the next Ansible run,
# Which knows neither pity nor remorse.

{% if httpd_is_systemd %}
sudo /usr/bin/systemctl status {{ httpd_servicename }}
{% else %}
sudo /sbin/service {{ httpd_servicename }} status
{% endif %}